spring:
  profiles:
    active: dev
  application:
    name: gulimall-order
  cloud:
    nacos:
      config:
        namespace: gulimall
        file-extension: yml
        server-addr: 101.200.164.72:8848
        group: dev
        extension-configs[0]:
          data-id: gulimall-order-dev.yml
          group: dev
          refresh: true